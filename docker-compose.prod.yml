services:
  frontend:
    build:
      context: .
      args:
        ENV: production
        VITE_GEMINI_API_KEY: ${VITE_GEMINI_API_KEY}
    ports:
      - "80:8080"
    environment:
      - NODE_ENV=production
    env_file: .env.production
